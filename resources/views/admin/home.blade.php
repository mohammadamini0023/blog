@extends('layouts.app') @extends('layouts.admin')
<html>
    <head></head>
    <body>
        {{-- @foreach($product as $products)
        <img src="{{ asset('/uploads/'.$products->upload_image()->path) }}" alt="" width="100">
        @endforeach --}}
    </body>
</html>

